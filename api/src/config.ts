import merge from 'lodash.merge'
import fs from 'fs'

const config = {
    name : 'test',
    api : {
        port :80,
    },
    database : {
        username : 'testmongoDB',
        password : '15918Petch',
        host : 'testmongoDB',
        port : 27017,
        database : 'test'
    }

}
